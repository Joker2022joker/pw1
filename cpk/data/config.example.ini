## this file is being looked for at ~/.config/cpk/config.ini

[main]
# password_generator = apg
## ^ a shell command
## does not perform any shell expansions

# db_path = ~/.config/cpk/wallet.asc
## ^ file path to the database file"

# input_kill_0x0a = on
# output_kill_0x0a = on

[loggers]
keys=root
[formatters]
keys=generic
[handlers]
keys=console
[logger_root]
handlers = console
qualename = root
levelno = 100

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(pathname) %(lineno) %(asctime)s %(levelname)-5.5s [%(name)s][%(threadName)s] %(message)s
